#include <stdio.h> 
#include <ctype.h>

#define ELEVER 5 
#define PROV 13 
struct Elev {
char namn [11];
double medel; 
}

int main () 
{
for (int i = 0; i < ELEVER; i++) 
{
int sum = 0;
scanf("%10s", elever[i].namn)

elever.namn[0]
toupper(elever[i].namn[0]);
 

for (int j = 0; j < PROV; j++) 
{ scanf("%d" , &poäng[i][j]);
sum = poäng [i][j];
}
medel[i] = (double)sum / PROV;
total += medel[i];

namn [i][0] = toupper(namn[i][0]);
for(int k = 1; namn[i][k] != '\0'; k++)
{ 





  

